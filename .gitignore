# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Node (for submodule builds)
node_modules/
pnpm-lock.yaml

# JMA raw / fragmented source files (only clean merged asset is committed)
# .py scripts and READMEs inside rawdata/ are still tracked
jma/rawdata/*.csv
jma/orig-minset/*.csv
jma/*_rawdata/*.csv
jma/jma_*_hourly_8.csv

# Analysis outputs (regenerated by running the script)
output/
